{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Flask App (venv)",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/app.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "justMyCode": true,
            "env": {
                "FLASK_ENV": "development"
            }
        }
    ]
}
